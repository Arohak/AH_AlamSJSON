# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

inhibit_all_warnings!

target 'AlamSJSON' do
    use_frameworks!
    pod 'Alamofire', '~> 1.2'
    pod 'SwiftyJSON', '~> 2.2.1'
    pod 'SwiftLoader', '~> 0.2'

    #don't use framework from here:
    pod 'PureLayout', '~> 2.0'
end

target 'AlamSJSON' do

end

